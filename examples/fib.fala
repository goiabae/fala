let var max = read 0;
    var fs [max]
in do
  write (fs [0] = 0);
	write "\n";
  write (fs [1] = 1);

  for var i from 2 to max-1 then do
	  write "\n";
  	write (fs [i] = (fs [i-1]) + (fs [i-2]))
  end;
	0
end
